#include <stdio.h>
int main()
{
   int num1,num2,product;
   printf("Enter the two numbers:);
   scanf("%d %d",&num1,&num2);
   product=num1*num2;
   printf("The product of the two numbers is: %d",product);
}
